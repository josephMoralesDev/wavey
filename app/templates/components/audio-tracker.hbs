<audio
  class="audio-tracker__controller"
  id={{this.args.audioId}}
  src={{this.args.link}}
  {{did-insert this.visualizerSetup}}
  {{will-destroy this.cleanup}}>
  <source class="audio-tracker__source" type="audio/x-m4a"/>
</audio>
{{yield (hash bufferArray=this.bufferArray)}}
